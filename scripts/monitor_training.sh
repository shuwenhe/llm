#!/bin/bash
# 训练监控脚本

echo "════════════════════════════════════════════════════════════════"
echo "             📊 中文文本训练监控"
echo "════════════════════════════════════════════════════════════════"
echo ""

# 监听输出
tail -f /home/shuwen/llm/training.log 2>/dev/null | while read line; do
  # 显示重要信息
  if echo "$line" | grep -E "(加载|Tokenizing|开始训练|Epoch|损失|完成|Error|错误)" > /dev/null; then
    echo "[$(date '+%H:%M:%S')] $line"
  fi
done
